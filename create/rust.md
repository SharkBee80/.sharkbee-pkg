update rust

```sh
rustup update stable
```

```sh
cargo new hello-rust # 创建新项目
cargo build # 可以构建项目
cargo run # 可以运行项目
cargo test # 可以测试项目
cargo doc # 可以为项目构建文档
cargo publish # 可以将库发布到 crates.io。
cargo --version # 显示当前 Rust 的版本信息
```
